# Django 환경 설정
# 프로덕션에서는 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# 보안 키 (필수) - 프로덕션에서는 반드시 복잡한 랜덤 문자열로 변경
SECRET_KEY=your-very-long-and-random-secret-key-here

# 디버그 모드 (기본값: False)
DEBUG=False

# 허용된 호스트 (기본값: localhost,127.0.0.1)
# 프로덕션에서는 실제 도메인으로 변경
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# HTTPS 사용 여부 (기본값: False)
# HTTPS가 설정된 프로덕션 환경에서만 True로 설정
USE_HTTPS=True